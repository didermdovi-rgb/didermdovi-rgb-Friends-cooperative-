# ---------------------------------------------------------------------------
# Friends Cooperative — WhatsApp receipt backend
# Copy this file to .env and fill in real values. NEVER commit the real .env.
# ---------------------------------------------------------------------------

# Server port
PORT=3000

# WhatsApp Business Cloud API (Meta) — from your Meta App / WhatsApp Business Platform setup
WA_PHONE_NUMBER_ID=YOUR_PHONE_NUMBER_ID
WA_ACCESS_TOKEN=YOUR_ACCESS_TOKEN
GRAPH_API_VERSION=v20.0

# Optional: approved WhatsApp template to use for business-initiated sends that fall
# outside the 24-hour customer service window (leave blank to always send as a
# free-form document message, which only works inside that window).
WA_TEMPLATE_NAME=
WA_TEMPLATE_LANGUAGE=bn

# Shared secret the frontend must send as the "X-Admin-Key" header on every request
# to /api/send-receipt. Set this to a long random string and put the same value
# into the Friends Cooperative app under Settings → WhatsApp Cloud API → Admin API Key.
ADMIN_API_KEY=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

# Comma-separated list of origins allowed to call this API (your app's URL(s)).
# Use * only for local testing.
ALLOWED_ORIGINS=*
