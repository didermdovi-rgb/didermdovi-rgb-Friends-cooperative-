{
  "name": "friends-cooperative-whatsapp-backend",
  "version": "1.0.0",
  "description": "Backend that sends Friends Cooperative PDF receipts directly to a member's WhatsApp via the official WhatsApp Business Cloud API.",
  "main": "server/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=18"
  }
}
